//-html
  //-body
ion-view(title='Dashboard')
  //ion-content.padding
  ion-pane(ng-controller='CardsCtrl')
    h5(ng-show='cards.length == 0') Looks like you swiped on all your friends! Invite some more to join the fun!
    td-cards
      td-card(ng-repeat='card in cards', on-destroy='cardDestroyed($index)', on-card-swipe='cardSwiped($index)', ng-controller='CardCtrl', on-swipe-left='cardSwipedLeft(card.name)', on-swipe-right='cardSwipedRight(card.name)')
        div
          .card
            h2 {{card.name}}
            //i.ion-person(style='font-size: 240px')
            //h4 {{card.title}}
            .image
              img(ng-src='{{card.url}}')
            .card-footer
            .yes-text(ng-style='leftTextOpacity') Like
            .no-text(ng-style='rightTextOpacity') Nope



                //-button.button.button-clear.button-positive(ng-click='goAway()') Answer
                //-button.button.button-clear.button-positive(ng-click='goAway()') Decline
