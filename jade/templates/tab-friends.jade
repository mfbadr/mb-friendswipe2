html
  body
    ion-view(title='Matches')
      ion-content
        ion-list
          ion-item(ng-repeat='match in matches', type='item-text-wrap', href='#/tab/friend/{{match.sender_id}}')
            | {{match.sender_name}}
            | - Matched on: {{match.time | date : format : 'mediumTime'}}
